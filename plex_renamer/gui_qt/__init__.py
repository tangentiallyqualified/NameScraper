"""PySide6 shell for Plex Renamer (GUI3).

This package is the Phase 3+ replacement for the tkinter shell in
``plex_renamer.gui``.  All orchestration goes through the application
layer (``plex_renamer.app.controllers``) — widget code handles only
presentation and user interaction.

Subpackages (created as phases progress):
    widgets/     — panel and tab widgets
    dialogs/     — modal and inline dialogs
    models/      — Qt item models and delegates
    resources/   — QSS theme, icons, assets
"""
