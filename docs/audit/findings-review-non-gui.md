# Audit findings review: non-GUI

## Scope and counts

This is a curated companion to the generated `docs/audit/maps/overview.md` checklist.

Reviewed the 73 raw findings in `.audit/analysis.json` whose source is not Radon and whose path does not start with `plex_renamer/gui_qt/`: 62 Vulture, 9 Ruff, and 2 contracts findings. The evidence file was generated at commit `486aaef`; `git diff --name-only 486aaef..HEAD -- plex_renamer` is empty, so the reviewed production sources are unchanged. Radon measurements were intentionally excluded.

## Methodology

Each raw record was retained as its own row in source order. For every candidate, the definition/import and surrounding control flow were inspected, followed by repository-wide exact-name searches through production code, tests, scripts, re-export modules, dataclass construction/serialization paths, framework or protocol hooks, and relevant extraction history where compatibility intent was ambiguous. A test-only reference was not treated mechanically: deliberate test seams and documented public surfaces were retained, while an orphaned implementation whose former production caller was replaced was confirmed dead. No cross-tool Ruff/Vulture duplicate exists in this assigned set; the two Ruff rows at `tv_library_discovery_service.py:8` are distinct imports, and the `_tv_scanner.py:15` import is distinct from the live same-named class method at line 266.

| Source | Candidate | Verdict | Evidence | Recommendation |
|---|---|---|---|---|
| Vulture | `plex_renamer/_job_store_db.py:58` — unused attribute `row_factory` | FALSE_POSITIVE | `connect_job_store` installs the SQLite row protocol at `plex_renamer/_job_store_db.py:52-59`; callers then require name lookup at `plex_renamer/_job_store_db.py:74` and `:84`. | allowlist |
| Vulture | `plex_renamer/_mkv_probe.py:31` — unused variable `is_forced` | FALSE_POSITIVE | This is a `MediaTrack` dataclass/schema field populated from mkvmerge JSON at `plex_renamer/_mkv_probe.py:65-73` and supplied by downstream test fixtures such as `tests/test_automux_service.py:54`. | retain/document |
| Vulture | `plex_renamer/_mkv_probe.py:39` — unused variable `container_type` | FALSE_POSITIVE | `parse_identify_json` populates the `ProbeResult` field at `plex_renamer/_mkv_probe.py:74-77`, and the parsed value is asserted at `tests/test_mkv_probe.py:35`. | allowlist |
| Vulture | `plex_renamer/_mkv_probe.py:80` — unused function `clear_probe_cache` | FALSE_POSITIVE | This is a deliberate cache-reset test seam called at `tests/test_mkv_probe.py:67` and `:86` and `tests/test_mkvmerge_integration.py:41` and `:73`. | allowlist |
| Vulture | `plex_renamer/app/controllers/media_controller.py:96` — unused attribute `_movie_discovery` | CONFIRMED | Repository-wide search finds only the assignment; movie startup routes through `MediaControllerMovieWorkflow.start_batch` at `plex_renamer/app/controllers/_controller_movie_workflows.py:19-26` without reading the injected service. | remove |
| Vulture | `plex_renamer/app/controllers/media_controller.py:371` — unused method `accept_tv_show` | FALSE_POSITIVE | `MediaController` is exported at `plex_renamer/app/controllers/__init__.py:10-18`; this public single-show entry point delegates at `media_controller.py:382-387` and is exercised at `tests/test_media_controller.py:250`, `:271`, `:293`, and `:656`. | retain/document |
| Vulture | `plex_renamer/app/controllers/queue_controller.py:89` — unused property `pending_count` | FALSE_POSITIVE | Public controller state is read at `tests/test_queue_controller.py:305` and `:335`; the property combines pending and running store counts at `queue_controller.py:90-92`. | retain/document |
| Vulture | `plex_renamer/app/controllers/queue_controller.py:96` — unused method `add_single_job` | FALSE_POSITIVE | This public queue API delegates to `add_single_queue_job` at `queue_controller.py:116-130` and is directly exercised at `tests/test_queue_controller.py:53`, `:88`, and `:219`. | retain/document |
| Vulture | `plex_renamer/app/controllers/queue_controller.py:183` — unused method `record_completed_job` | CONFIRMED | There is no call site; its own contract says it serves the legacy direct-rename/tkinter path while PySide uses queue execution at `queue_controller.py:184-190`. The repository now contains only the Qt shell. | remove |
| Vulture | `plex_renamer/app/controllers/queue_controller.py:197` — unused method `get_latest_revertible_job` | CONFIRMED | Repository-wide search finds only this wrapper; current code calls neither it nor a dynamic equivalent, while it merely forwards to `JobStore.get_latest_completed_with_undo` at `queue_controller.py:197-199`. | remove |
| Vulture | `plex_renamer/app/models/state_models.py:84` — unused property `is_active` | CONFIRMED | No read exists in production, tests, or scripts; progress consumers use `ScanProgress.lifecycle` (for example `plex_renamer/gui_qt/_main_window_scan.py:35`) rather than this derived property. | remove |
| Vulture | `plex_renamer/app/models/state_models.py:106` — unused variable `last_accessed_at` | FALSE_POSITIVE | This exported `CacheEntry` schema field is hydrated from the SQLite access timestamp at `plex_renamer/app/services/cache_service.py:104-120` and emitted again by `put` at `:174-183`. | allowlist |
| Vulture | `plex_renamer/app/models/state_models.py:129` — unused property `is_fresh` | CONFIRMED | Repository-wide search finds only the definition; consumers inspect `CacheLookup.state`/`entry`, and `PersistentCacheService.get` returns those directly at `cache_service.py:122-125`. | remove |
| Vulture | `plex_renamer/app/models/state_models.py:144` — unused variable `eligible_job_count` | FALSE_POSITIVE | `CommandGatingService` populates the exported result field at `plex_renamer/app/services/command_gating_service.py:118` and `:215`; its batch value is asserted at `tests/test_command_gating_service.py:242`. | allowlist |
| Vulture | `plex_renamer/app/models/state_models.py:153` — unused variable `mapped_episodes` | FALSE_POSITIVE | `EpisodeMappingService` computes this structured summary value at `plex_renamer/app/services/episode_mapping_service.py:406-408`, and `tests/test_episode_mapping_projection.py:73` reads it. | allowlist |
| Vulture | `plex_renamer/app/models/state_models.py:156` — unused variable `missing_episodes` | FALSE_POSITIVE | The field is computed from guide rows at `episode_mapping_service.py:410` and asserted at `tests/test_episode_mapping_projection.py:75` and `:155`. | allowlist |
| Vulture | `plex_renamer/app/models/state_models.py:160` — unused variable `review_required` | FALSE_POSITIVE | This is part of the exported `EpisodeGuideSummary` schema and is explicitly populated from review rows at `plex_renamer/app/services/episode_mapping_service.py:406-415`; Vulture misses the dataclass keyword-field use. | allowlist |
| Vulture | `plex_renamer/app/models/state_models.py:178` — unused property `episode_key` | FALSE_POSITIVE | The property is directly read for mapped and missing rows at `tests/test_episode_mapping_projection.py:79` and `:84`; `EpisodeGuideRow` is also exported at `app/models/__init__.py:7` and `:26`. | retain/document |
| Vulture | `plex_renamer/app/models/state_models.py:188` — unused variable `ignored` | CONFIRMED | No constructor supplies the field and no production/test/script read or assignment exists; the only repository hit for this symbol in the model is its declaration. | remove |
| Vulture | `plex_renamer/app/models/state_models.py:213` — unused variable `source_label` | FALSE_POSITIVE | `EpisodeMappingService` populates the exported guide field at `plex_renamer/app/services/episode_mapping_service.py:313-316`, and `tests/test_episode_mapping_projection.py:72` asserts it. | allowlist |
| Ruff | `plex_renamer/app/services/_tv_library_classification.py:126` — F841 `has_direct_season_subdirs` | CONFIRMED | The local is initialized at line 118 and assigned at line 126 but never read; returned `ClassifiedDirectory` instances use hard-coded booleans at lines 148, 159, 170, 181, 199, 210, and 220. | remove |
| Vulture | `plex_renamer/app/services/cache_service.py:47` — unused attribute `row_factory` | FALSE_POSITIVE | `_connect` installs `sqlite3.Row` at `cache_service.py:44-48`; subsequent code requires named columns at `:110-119`, `:232-243`, and `:265`. | allowlist |
| Vulture | `plex_renamer/app/services/cache_service.py:81` — unused method `make_key` | FALSE_POSITIVE | `PersistentCacheService` is an explicit public service (`app/services/__init__.py:3`, `:15`), and this stable-key API is directly tested at `tests/test_cache_service.py:233`. | retain/document |
| Vulture | `plex_renamer/app/services/cache_service.py:185` — unused method `mark_refreshing` | FALSE_POSITIVE | This public cache-state operation persists the `is_refreshing` model field at `cache_service.py:185-191` and is exercised at `tests/test_cache_service.py:110`, `:114`, and `:258`. | retain/document |
| Vulture | `plex_renamer/app/services/cache_service.py:202` — unused method `invalidate_namespace` | FALSE_POSITIVE | This is a tested public invalidation operation: `tests/test_cache_service.py:133` calls it and checks the deletion count; the service class is exported at `app/services/__init__.py:15`. | retain/document |
| Vulture | `plex_renamer/app/services/cache_service.py:221` — unused method `invalidate_by_prefix` | FALSE_POSITIVE | The public prefix invalidation path is directly exercised at `tests/test_cache_service.py:210`; implementation and row-count contract are at `cache_service.py:221-228`. | retain/document |
| Vulture | `plex_renamer/app/services/episode_mapping_service.py:130` — unused method `apply_assignments` | FALSE_POSITIVE | This documented compatibility API delegates to `apply_bulk` at `episode_mapping_service.py:130-138` and is called repeatedly at `tests/test_episode_mapping_projection.py:346-450`. | retain/document |
| Vulture | `plex_renamer/app/services/episode_projection_cache.py:24` — unused property `cache_size` | FALSE_POSITIVE | This is a deliberate cache-observability test seam read at `tests/test_episode_projection_cache.py:38`, `:57`, and `:112`. | allowlist |
| Vulture | `plex_renamer/app/services/refresh_policy_service.py:27` — unused variable `retry_after_seconds` | FALSE_POSITIVE | `can_manual_refresh` populates the dataclass field at `refresh_policy_service.py:135-140`, and the client contract is asserted at `tests/test_refresh_policy_service.py:183`. | allowlist |
| Vulture | `plex_renamer/app/services/refresh_policy_service.py:102` — unused method `should_background_refresh` | FALSE_POSITIVE | `RefreshPolicyService` is explicitly exported at `app/services/__init__.py:7`, `:16`; this public policy is exercised for missing/fresh/stale cases at `tests/test_refresh_policy_service.py:128-154`. | retain/document |
| Vulture | `plex_renamer/app/services/refresh_policy_service.py:119` — unused method `can_manual_refresh` | FALSE_POSITIVE | The exported service's cooldown API is called at `tests/test_refresh_policy_service.py:165-188`, including validation of its structured retry result. | retain/document |
| Vulture | `plex_renamer/app/services/refresh_policy_service.py:142` — unused method `get_rescan_scope` | FALSE_POSITIVE | The exported service's path-scoping API is exercised for child, root, and outside-library paths at `tests/test_refresh_policy_service.py:203-217`. | retain/document |
| Vulture | `plex_renamer/app/services/settings_service.py:81` — unused property `match_country` | FALSE_POSITIVE | This typed `SettingsService` API derives the country from `match_language` at lines 81-87 and is asserted for `US` and `FR` at `tests/test_alt_title_matching.py:834` and `:843`. | retain/document |
| Ruff | `plex_renamer/app/services/tv_library_discovery_service.py:8` — F401 `get_season` | CONFIRMED | The service delegates classification to `TVDirectoryClassifier` at lines 39 and 170-171; the only `get_season` occurrence in this module is the import/comment, so the import can be removed. This is paired with, not duplicate of, the next Ruff row. | remove |
| Ruff | `plex_renamer/app/services/tv_library_discovery_service.py:8` — F401 `is_extras_folder` | CONFIRMED | No reference exists after the line-8 import; extras/season classification lives in `_tv_library_classification.py`. This is a distinct import finding sharing a source line with the preceding Ruff row. | remove |
| Vulture | `plex_renamer/app/services/tv_library_discovery_service.py:173` — unused method `_counts_as_season_subdir` | CONFIRMED | Repository-wide search finds only this private forwarding wrapper; active discovery calls `_classifier` directly at lines 39, 82, 136, 171, and 196. | remove |
| Vulture | `plex_renamer/app/services/tv_library_discovery_service.py:176` — unused method `_scan_children` | CONFIRMED | The private wrapper has no call site; active child enumeration uses `_iter_child_dirs` at line 107 or classifier-owned scanning through `_classify_directory` at lines 136 and 170-171. | remove |
| Vulture | `plex_renamer/app/services/tv_library_discovery_service.py:198` — unused method `_child_title_matches_parent` | CONFIRMED | Repository-wide search finds only this private forwarding definition; title matching is owned by `TVDirectoryClassifier.child_title_matches_parent`, with no service-level caller. | remove |
| Vulture | `plex_renamer/constants.py:67` — unused variable `SUBTITLE_DOWNLOAD` | FALSE_POSITIVE | This is an intentional reserved member of public `JobKind`; the adjacent contract documents the future network job at `constants.py:57-67`, and `job_store.py:140` documents its payload slot. | retain/document |
| Contracts | `plex_renamer/engine/_batch_orchestrators.py:1` — forbidden import `plex_renamer.app.services` | CONFIRMED | The engine lazily imports TV and movie application services at `engine/_batch_orchestrators.py:159` and `:873`; `scripts/audit/contracts.toml:8-11` explicitly forbids every `engine -> app` edge, including lazy imports. | architectural follow-up |
| Vulture | `plex_renamer/engine/_batch_orchestrators.py:150` — unused method `_boost_tv_scores_with_episode_evidence` | CONFIRMED | The private wrapper has no call site; active evidence adjustment calls the module-level implementation from `engine/matching.py:614`, leaving only this definition/forward at lines 150-155. | remove |
| Vulture | `plex_renamer/engine/_batch_orchestrators.py:751` — unused method `is_tv_library` | CONFIRMED | Repository-wide search finds only the definition; current library detection is provided by `TVLibraryDiscoveryService`, injected through `_get_discovery_service` at lines 157-162. | remove |
| Vulture | `plex_renamer/engine/_batch_orchestrators.py:878` — unused method `discover_movies` | FALSE_POSITIVE | `BatchMovieOrchestrator` is public at `engine/__init__.py:31-34` and `:62`; the discovery entry point is exercised at `tests/test_alt_title_matching.py:546` and `:633` and `tests/test_batch_autoaccept_guards.py:144`. | retain/document |
| Vulture | `plex_renamer/engine/_batch_orchestrators.py:1101` — unused method `rematch_movie` | CONFIRMED | No production/test/script call exists; current application rematching routes through `MediaControllerMovieWorkflow.rematch_state` at `app/controllers/_controller_movie_workflows.py:43-59`. | remove |
| Contracts | `plex_renamer/engine/_movie_scanner.py:1` — forbidden import `plex_renamer.app.services` | CONFIRMED | `_filter_tv_show_root_files` imports and constructs `TVLibraryDiscoveryService` at `engine/_movie_scanner.py:127-129`; the blanket `engine -> app` prohibition is stated at `scripts/audit/contracts.toml:8-11`. | architectural follow-up |
| Vulture | `plex_renamer/engine/_movie_scanner.py:105` — unused property `explicit_files` | FALSE_POSITIVE | This public scanner inspection property returns constructor state at lines 93-108 and is asserted for multi-file discovery at `tests/test_alt_title_matching.py:642`. | retain/document |
| Vulture | `plex_renamer/engine/_mux_planner.py:57` — unused variable `output_name` | FALSE_POSITIVE | `MuxPlan.to_dict` serializes all dataclass fields with `asdict` at lines 78-79; serialized consumers read `output_name` at `engine/_queue_bridge.py:74`, and object use is asserted at `tests/test_mux_planner.py:154`. | allowlist |
| Vulture | `plex_renamer/engine/_mux_planner.py:64` — unused variable `user_modified` | FALSE_POSITIVE | The field is serialized/deserialized by `MuxPlan.to_dict/from_dict` at lines 78-88; application code consumes the serialized key at `app/services/automux_service.py:134`, and the GUI writes it at `gui_qt/widgets/_automux_tracks.py:310`. | allowlist |
| Ruff | `plex_renamer/engine/_tv_scanner.py:15` — F401 `build_consolidated_preview` | CONFIRMED | The imported alias is never referenced. The live same-named class method at line 266 instead imports/calls `build_consolidated_table` at lines 281-290, so this is not a Vulture duplicate and the stale import should be removed. | remove |
| Ruff | `plex_renamer/engine/_tv_scanner.py:26` — F401 `SeasonCompleteness` | CONFIRMED | No use exists in `_tv_scanner.py`; completeness construction imports the type in `engine/_tv_scanner_postprocess.py:19` and uses it there at line 79. | remove |
| Vulture | `plex_renamer/engine/_tv_scanner.py:121` — unused method `invalidate_cache` | CONFIRMED | Repository-wide search finds only this definition; scan reset replaces scanner/state through `ScanState.reset_scan` at `engine/models.py:304-311` rather than invalidating this instance. | remove |
| Vulture | `plex_renamer/engine/_tv_scanner.py:174` — unused method `get_mismatch_info` | CONFIRMED | No call exists in production, tests, scripts, or dynamic lookup; current scan code consumes `_detect_mismatch` internally at lines 161 and 190-198. | remove |
| Vulture | `plex_renamer/engine/episode_assignments.py:21` — unused variable `ROLE_VERSION` | FALSE_POSITIVE | The constant is an explicitly reserved extension discriminator (`episode_assignments.py:7-21`) and its merge round-trip contract is exercised at `tests/test_episode_assignments.py:248` and `:272-286`. | retain/document |
| Vulture | `plex_renamer/engine/episode_assignments.py:254` — unused method `unclaimed_slots` | FALSE_POSITIVE | The query is called by `scripts/scan_real_library.py:130` and validated at `tests/test_episode_assignments.py:139`; it is a live inspection API on the assignment table. | retain/document |
| Vulture | `plex_renamer/engine/episode_assignments.py:264` — unused function `ingest_preview_items` | CONFIRMED | Only legacy unit tests call it (`tests/test_episode_assignments.py:191-244`); the production consolidated path now builds the table directly at `engine/_tv_scanner.py:281-290`, leaving this former adapter orphaned. | remove |
| Vulture | `plex_renamer/engine/models.py:80` — unused method `is_move` | CONFIRMED | Repository-wide search finds only the method definition; consumers compare `target_dir`/`original` directly or use `PreviewItem.is_actionable`, so this unreferenced helper is removable. | remove |
| Vulture | `plex_renamer/engine/models.py:269` — unused property `match_pct` | CONFIRMED | No read exists in production, tests, or scripts; completeness consumers use the report/seasons directly, while the property merely forwards `completeness.pct` at lines 269-273. | remove |
| Vulture | `plex_renamer/engine/models.py:275` — unused property `all_skipped` | CONFIRMED | Repository-wide search finds only the definition; active orchestration computes actionability directly, for example `engine/_batch_orchestrators.py:1074-1076`. | remove |
| Vulture | `plex_renamer/engine/models.py:288` — unused property `actionable_file_count` | CONFIRMED | No read exists; the neighboring `actionable_indices` property at lines 281-286 is the live API and this property only takes its length. | remove |
| Vulture | `plex_renamer/engine/show_details.py:26` — unused variable `first_air_date` | FALSE_POSITIVE | This is part of the provider-neutral `ShowDetails` dataclass contract, populated by the TMDB adapter at `show_details.py:43-50` and asserted at `tests/test_show_details.py:41`. | allowlist |
| Ruff | `plex_renamer/job_store.py:19` — F401 `json` | CONFIRMED | Serialization is delegated to `_job_store_codec` imports at `job_store.py:30-36`; no `json` name use remains in the module. | remove |
| Ruff | `plex_renamer/job_store.py:27` — F401 `typing.Any` | CONFIRMED | No annotation or runtime reference uses `Any` in `job_store.py`; repository-wide exact-name inspection confirms the import is isolated. | remove |
| Vulture | `plex_renamer/job_store.py:207` — unused property `is_terminal` | CONFIRMED | No read exists in production, tests, or scripts; terminal filtering is performed in SQL, for example `job_store.py:505-513` and `:599-607`. | remove |
| Vulture | `plex_renamer/job_store.py:274` — unused method `_migrate_db` | CONFIRMED | Initialization calls extracted `initialize_job_store` directly at `job_store.py:266-272`, which calls `migrate_job_store` at `_job_store_db.py:74-75`; this private compatibility wrapper has no caller. | remove |
| Vulture | `plex_renamer/job_store.py:428` — unused method `remove_job` | CONFIRMED | Repository-wide search finds no call; the controller and current UI use the bulk path `QueueController.remove_jobs` at `queue_controller.py:223-225`, backed by `JobStore.remove_jobs` at `job_store.py:443-461`. | remove |
| Vulture | `plex_renamer/job_store.py:463` — unused method `reorder_job` | FALSE_POSITIVE | The single-job ordering API is directly exercised at `tests/test_queue_controller.py:698` and delegates to the extracted ordering helper at `job_store.py:463-474`; it remains a supported public store operation alongside bulk moves. | retain/document |
| Vulture | `plex_renamer/job_store.py:515` — unused method `_compact_positions` | CONFIRMED | No caller remains after ordering extraction; the wrapper only forwards to `compact_positions` at lines 515-517, while active ordering uses `reorder_pending_job`/`move_pending_jobs` at lines 468-492. | remove |
| Vulture | `plex_renamer/job_store.py:575` — unused method `_rebase_path` | CONFIRMED | Repository-wide search finds only this private compatibility wrapper; active propagation calls `rewrite_job_paths` at `job_store.py:551-556`, which owns rebasing in `_job_path_propagation.py`. | remove |
| Vulture | `plex_renamer/job_store.py:593` — unused method `get_running` | CONFIRMED | No caller exists; current queue consumers request combined pending/running rows through `get_queue` at `job_store.py:596-597`. | remove |
| Vulture | `plex_renamer/job_store.py:658` — unused method `get_queued_tmdb_ids` | CONFIRMED | No production/test/script reference exists; queued-state consumers operate on queue/job projections rather than this ID-only query. | remove |
| Ruff | `plex_renamer/tmdb.py:28` — F401 `TMDBAPIError` | FALSE_POSITIVE | This preserves the exception at the historical public `plex_renamer.tmdb` namespace after transport extraction: `tmdb.py:27-33` re-exports the transport types, while the implementation is now at `_tmdb_transport.py:25`. | allowlist |
| Ruff | `plex_renamer/tmdb.py:31` — F401 `TMDBRateLimitError` | FALSE_POSITIVE | Like `TMDBAPIError`, this import intentionally keeps the pre-extraction module API available (`tmdb.py:27-33`); the concrete class is at `_tmdb_transport.py:21`. Make the re-export explicit rather than deleting it. | allowlist |
| Vulture | `plex_renamer/tmdb.py:110` — unused method `_get` | CONFIRMED | Production methods call `_get_safe`/transport directly (`tmdb.py:114-116` and, e.g., `:244`); only tests call `_get` at `tests/test_tmdb.py:95`, `:114`, and `:131`, so this private pre-extraction wrapper and its stale direct tests can be removed or retargeted. | remove |

## Verdict totals

- FALSE_POSITIVE: 37
- CONFIRMED: 36
- UNCERTAIN: 0
- Total: 73

Source totals remain 62 Vulture + 9 Ruff + 2 contracts = 73.

## Cross-cutting patterns

- Vulture misses protocol-driven assignments (`sqlite3.Connection.row_factory`), dataclass keyword construction, `asdict` serialization, test seams, and deliberately supported public service/controller methods. These should be allowlisted narrowly or made explicit rather than removed.
- Several refactors left private forwarding wrappers and imports behind. The clearest clusters are TV discovery/classification, consolidated TV scanning, JobStore helper extraction, and the private TMDB transport wrapper.
- Test coverage alone did not rescue an obsolete implementation: `ingest_preview_items` is still tested, but its production caller was replaced by direct table construction. Conversely, cache reset/size hooks and public service methods are deliberate tested surfaces.
- Both contract findings are real despite lazy/local imports. Dependency injection or moving discovery orchestration above `engine` is required to restore the documented layer direction.
- The two TMDB exception imports are compatibility re-exports, not ordinary unused imports; explicit aliases/`__all__` entries (or targeted Ruff ignores) would communicate that intent.

## 2026-07-17 postscript

This partition's 36 `CONFIRMED` records, including both confirmed layer-contract violations noted above, are part of the review's 108-record total remediated on `dev/audit-debt3` (PRs #20-#23); see [findings-review.md](findings-review.md) for the full outcome. The generated checklist ([maps/overview.md](maps/overview.md)) has been regenerated since; these verdicts remain the unmodified historical record of the original triage and are not rewritten.
