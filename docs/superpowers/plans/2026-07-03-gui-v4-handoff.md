# GUI V4 — Session Handoff

> **Purpose:** live handoff so a fresh session can resume mid-stream. Update after every milestone.
> **Branch:** `dev/GUI4` (created 2026-07-03 from `dev/GUI3` @ 9794499)

## Current status (updated 2026-07-05, end of Plan 8 execution session)

- [x] Branch `dev/GUI4` created and checked out
- [x] Codebase exploration (findings below)
- [x] Design spec: [docs/superpowers/specs/2026-07-03-gui-v4-design.md](../specs/2026-07-03-gui-v4-design.md) — self-reviewed, committed
- [x] Roadmap index: [2026-07-03-gui-v4-implementation.md](2026-07-03-gui-v4-implementation.md) (9 plans mapped to spec sections)
- [x] Plan 1 (theme foundation + de-Plex + chrome fixes): [2026-07-03-gui-v4-plan1-theme-foundation.md](2026-07-03-gui-v4-plan1-theme-foundation.md) — complete, executable, TDD steps
- [x] User review gate cleared: user approved spec + Plan 1, confirmed app name **"NameScraper"**, capped execution at 4 subagents
- [x] **Plan 1 LANDED** (2026-07-03, commits `6c82086..d421d37`): theme.py tokens + theme.qss.tmpl (Jellyfin palette, fringes gone, radii normalized), all gui_qt hex → tokens with permanent no-hex guard, de-Plex strings ("NameScraper", "Fully Ready"/`fully-ready`) with AST guard, Ctrl+Z undo removed, recent-folder menus switch tabs. Final review: 0 Critical/Important; fast 928/928, smoke 163/163.
- [x] **Plan 2 LANDED** (2026-07-03, commits `9e366bb..e3bb6de` + carry-over commit): roster is now QListView + `RosterModel` + `RosterDelegate` (`widgets/_roster_model.py`, `_roster_delegate.py`, `status_chip.py`); `RosterRowWidget` deleted; new taxonomy live incl. `Specials & Unmapped Only` (`specials-unmapped`) with review groups above Matched; season chips + shown status pill + confidence bar+% on 64×94 poster rows; `is_plex_ready_state` → `is_fully_ready_state` repo-wide; splitter default [380, 500, 360]. Final review: 0 Critical, 1 Important (chip-tooltip font mismatch) fixed in `e3bb6de` + re-review clean. Fast 935/935, smoke 177/177.
- [x] Plan 3 (work panel + 2-panel cutover): [2026-07-03-gui-v4-plan3-work-panel.md](2026-07-03-gui-v4-plan3-work-panel.md) — written + self-reviewed, 7 TDD tasks (EpisodeTableModel / delegate+view / expansion card / MediaWorkPanel / cutover / sweep / verify). Notable scope calls: sticky season header dropped (strip supersedes), warm_preview_cache deletion pulled forward from Plan 5 (its host file dies in the cutover), "filenames inline" bound to existing view_mode, Unassign All stays plain until Plan 4.
- [x] **Plan 3 LANDED** (2026-07-03, commits `d8552ce..e3e7f57`): 2-panel workspace live — preview + detail panels deleted (7 modules incl. `_workspace_widgets`), replaced by `MediaWorkPanel` (show header w/ async overview, season strip, toolbar, footer) over `EpisodeTableModel`/`EpisodeTableDelegate`/`EpisodeTableView` with `EpisodeExpansionCard` as the single persistent-editor expansion; workspace aliases rebound so the action-bar system runs unchanged; splitter 2-pane [380, 860]; deleted-module guard + `_clear_fully_ready_checks` rename (`e6fb4f3`). Final review (test-drop audit reconciled, expansion lifecycle traced, 4 cutover deviations adjudicated KEEP): 1 Important (expansion sentinel survived `_rebuild`) fixed `8269b79`; Task-7 verification surfaced 3 more real defects, all fixed: footer breakdown violated spec §3.2.5 segments (`06106b2`), expansion editor floated as a detached top-level window because `createEditor` dropped its parent arg (`e3e7f57` + parentage regression test), status-pill QSS lacked the `warning` tone the header remap produces (same commit + tone-vocabulary guard). Fast 927/927, smoke 188/188, zero skips.
- [x] Plan 4 (bulk assign + unassign-all danger treatment): [2026-07-03-gui-v4-plan4-bulk-assign.md](2026-07-03-gui-v4-plan4-bulk-assign.md) — written + self-reviewed, 7 TDD tasks (service batch mutations / BulkAssignPanel / work-panel hosting + toolbar / workspace wiring + confirm + toast / problems-filter hint row / dialog full-filename / verify). Notable scope calls: exactly two additive `EpisodeMappingService` methods (`apply_assignments`, `unassign_all` — one reproject each; spec §6's "one batched call" vs §16's "no behavioral changes" resolved as additive composition of existing table mutations); staging is panel-local (nothing → controller until Apply); drag rejects claimed slots (displace stays a single-row act); new `toast_requested` signal → existing ToastManager; `match_picker_dialog` restyle NOT here (not §6 — rides with Plan 8/9).
- [x] **Plan 4 LANDED** (2026-07-04, commits `5ae5c96..eb8ee91`): Bulk Assign live — `EpisodeMappingService.apply_assignments`/`unassign_all` batch mutations (one reproject each); `BulkAssignPanel` (files/slots panes, check-then-assign-in-order walking free slots, auto-map remaining, drag pairs, staging as in-place `→` preview) hosted in a work-panel stack; overflow `…` entry + danger-outline Unassign All with exact-count confirm + bulk-assign offer; one `toast_requested` per Apply into ToastManager; problems-filter empty-state hint row routes into the mode; assign dialog shows full wrapped filenames. Final review With-Fixes → I1 (missing-table guard) + I2 (Apply re-read the live roster selection instead of the entered state) fixed in `eb8ee91`: bulk mode pins its state, a roster switch mid-staging auto-exits with staging discarded, same-state refreshes keep the mode (accepted boundary, see "How to resume") — re-review: Ready to merge — Yes. Fast 931/931, smoke 209/209, zero skips.
- [x] Plan 5 (async guide build + BusyOverlay + perf guards): [2026-07-04-gui-v4-plan5-async-perf.md](2026-07-04-gui-v4-plan5-async-perf.md) — written + self-reviewed, 7 TDD tasks (BusyOverlay+`busy_scope` / cache peek+build-with-signature+store / async model pipeline with skeleton rows + generation token / season-strip no-churn (Plan 3 M2) / overlay wiring bulk-apply+unassign-all+queue+tab-restore / permanent perf guards / verify). Notable scope calls: post-mutation `refresh_episode_guide` stays synchronous (overlay covers the interaction; async applies to displaying uncached states); signature captured before off-thread build so races degrade to stale-miss; async engages only when the workspace wires all three provider callables (bare panels keep the sync path); spec's "force rescan" overlay site doesn't exist in code — recorded deviation.
- [x] **Plan 5 LANDED** (2026-07-04, commits `53ba242..3765d5a`): async + perf live — `BusyOverlay` + `busy_scope()` (`widgets/busy_overlay.py`: ~120ms deferred show or `immediate=True` for blocked-loop sites, guaranteed finally-removal, dismiss reparents to None) wired at bulk apply / unassign-all / queue / tab-restore; `EpisodeProjectionCacheService` gained `cached_guide_for_state` peek + `build_guide_with_signature` (signature captured **before** the off-thread build, so concurrent mutation degrades to a stale miss, never a wrong hit) + `store_guide`; `EpisodeTableModel.show_state` on an uncached state renders skeleton rows and builds the guide on the shared pool with a generation-token + state-identity delivery gate (`guide_loaded` signal; cached states render synchronously; bare panels/fakes keep the sync path via `hasattr` guards); season strip skips rebuilds when chip specs are unchanged (Plan 3's deferred M2 closed); permanent perf guards (no GUI-thread guide builds, no `processEvents` in gui_qt, cached 300-ep switch budget — 18ms measured vs 500ms cap). Final review With-Fixes → I1 (cache-hit re-renders didn't bump the token, so a build made stale by a mid-flight mutation could render pre-mutation rows and clobber the fresh cache entry) + I2 (guide arriving mid-bulk re-showed the hidden Approve All/Unassign All) — **both plan defects implemented verbatim**, fixed in `3765d5a` (unconditional token bump at the top of `_resolve_guide_or_schedule`; `bulk_assign_active()` guard in `_on_guide_loaded`) with two discriminating regressions; fresh-reviewer re-review probed the fix (no orphaned-build skeleton path; all four bulk exits re-derive the toolbar; worktree RED corroborated): Ready to merge — Yes. Fast 936/936, smoke 229/229, zero skips.
- [x] Plan 6 (toasts + loading screen): [2026-07-04-gui-v4-plan6-toasts-loading.md](2026-07-04-gui-v4-plan6-toasts-loading.md) — written + self-reviewed, 7 TDD tasks (toast card rebuild / copy+sticky+hover / conveyor+stepper / status lines+quips / async polish / toolbar gating / verify). Notable scope calls: bulk apply-failure keeps exiting the mode (spec §17's stay-open needs an `apply_assignments` signature change — recorded open question); guide-failure roster pill stays engine-driven (view may not write `scan_error`); stepper active dot restyled warning→accent.
- [x] **Plan 6 LANDED** (2026-07-04, commits `eb5ea11..6793ccf`): toasts + loading screen live — `_ToastCard` rebuilt on theme QSS selectors (tone icon ✓/!/i, 3-line clamp with Show more expand-in-place capped at 40% of the window with internal scroll, Copy puts full `title\nmessage` on the clipboard even when clamped, errors sticky by default with explicit durations honored, hover pauses the countdown bar, `layout_changed` reflows the manager); `ScanProgressWidget` internals replaced — `_ConveyorAnimation` poster-card conveyor (filled wash behind a center accent beam) + `_PhaseStepper` dots (done=success / active=accent / pending=muted, tooltip carries the phase list) + single primary/secondary status lines (`ElidedLabel` middle-elide + tooltip; filler quips rotate on the secondary line only after 4s without item changes, honest phase always primary); Plan 5's deferred async polish closed — a failed guide build renders the retryable error section-label row + "Guide unavailable" footer via the new `guide_failed` bridge signal (reselect schedules a fresh build), the skeleton footer says "Loading episodes…", the queue envelope splits post-**success** sync failures into "Queued With Warnings" (still emits `queue_changed`), the bulk-apply failure toast goes error-tone with the skip reason, and `update_toolbar` is the single owner of bulk-mode button gating (re-review M6). Final review With-Fixes → I1 (scan-error states inherited a false "Loading episodes…" / path-dependent stale "Guide unavailable" footer — plan defect implemented verbatim) fixed in `6793ccf` with a stale-flag pinning test; re-review: Ready to merge — Yes. Fast 940/940, smoke 249/249, zero skips.
- [x] Plan 7 (queue/history restyle + companion surfacing): [2026-07-04-gui-v4-plan7-queue-history.md](2026-07-04-gui-v4-plan7-queue-history.md) — written + self-reviewed, 7 TDD tasks (Files column + token tints / pills + accent title + rows + danger-outline / pairing layer / detail-tree nesting / revert banner / empty states / verify). Notable scope calls: 5 recorded deviations — §11 stripe/selection/alternation and segmented filters and primary-button scheme already landed in Plan 1 (pinned by test, not re-implemented); companion↔video pairing is a **view-side heuristic** (same target dir + target-stem prefix, longest stem wins; residual "Companion Files (N)" group never drops a miss) because `RenameOp` persists no linkage and §16 forbids schema changes; the facts card keeps separate Files/Companions cells (merged format is table-column-only).
- [x] **Plan 7 LANDED** (2026-07-05, commits `c597dbb..bd9d579`): queue/history restyle live — delegate-painted status pills in the workspace idiom (tone-token 12% wash, pill radius, uppercase tone text, fixed 18px height) with a `sizeHint` override so ResizeToContents reserves the uppercase advance + padding; accent title on the highlighted row (fresh per-paint option copy, no palette leak); 36px rows; danger-outline Remove/Revert Selected; merged Files column `3 files (2 comp.)` (singular + zero-companion forms, `files_cell_text`); `_TRANSITION_COLORS` token-derived; companion renames nested under their video in the detail tree with SUB/NFO-style type badges (§13 merge-grouping seam) + residual group only for unpaired ops + the `53e9900` guard keeping group-header arrow prefixes off video rows (identity predicate on UserRole, non-vacuous regression); revert confirmation is a warning banner (rgba warning wash + `${warning}` border, Confirm danger / Cancel secondary) directly above the actions bar; illustrated empty states (queue/history/filter-no-match) on a QStackedWidget that leaves `select_job`/context-menu paths untouched. Final review With-Fixes → I1 (Status section sized from the mixed-case display text → COMPLETED/CANCELLED pills lost padding, REVERT FAILED clipped ~8px of glyphs) fixed in `bd9d579`: delegate `sizeHint` reserves uppercase advance + 2×8px pad + 4px cell margin, pill height pinned to `_scale.px(18)` (workspace `_PILL_H_U` parity, review M2), REVERT_FAILED regression test, `import unittest` fixed en passant (review M1). Task 7 grabs extended per review Rec 2 with REVERT_FAILED/CANCELLED: Status section = 106px = exact widest-pill requirement at the real QSS font. Fast 947/947, smoke 261/261, zero skips.
- [x] Plan 8 (settings restyle + mkvmerge seams): [2026-07-05-gui-v4-plan8-settings-seams.md](2026-07-05-gui-v4-plan8-settings-seams.md) — written + self-reviewed, 6 tasks (check-glyph SVG via new `${check_svg}` template path token / section-card icon+title header rows / Cache→Data merge with danger-outline + confirm counts / hidden Tools shell / test-only pinning of the Plan-3 expansion-card seams / verify). Notable scope calls: 7 recorded deviations — §12's combo/slider/input restyle landed in Plan 1 and §13's expanded-row seams (badges, disabled Merge…, Part chips) landed in Plan 3 (pinned here, not re-implemented); §16's dialog restyle is a no-op (both dialogs already token-clean); the checked indicator keeps its `${success}` fill (spec names no color); the Cache nav page merges into Data (stats move with the destructive buttons); Part chips are a latent seam under today's all-multi-claims-are-conflicts policy, so the pin test uses a future-`ROLE_VERSION` stub table; the one new hook is the additive `history_count_callback` (§16-clean read-model wiring for confirm counts).
- [x] **Plan 8 LANDED** (2026-07-05, commits `dd3e647..cb24bd7`): settings restyle + mkvmerge seams live — themed checkbox indicator with a DPI-crisp SVG check glyph (`resources/check.svg`, named colors only; new `${check_svg}` absolute-posix template token so string-stylesheet `url()` needs no CWD; unchecked well moves to `input_bg`); section cards swap the heading+hairline for icon+title header rows (QStyle standard icons at `_scale.icon("sm")`, all six pages); destructive actions consolidated under a merged "Data" page (Cache nav row gone, stats moved along) as danger-outline behind confirm-with-count dialogs (cache count from `stats()["item_count"]` pre-clear, history count via the additive `history_count_callback` → `len(queue_ctrl.get_history())`; zero-count paths short-circuit to a caption; injected message-box pattern throughout — nothing reaches a raw QMessageBox offscreen); hidden "Tools" nav row + reserved page (§13 seam, placeholder names mkvmerge); Plan-3 expansion-card seams pinned test-only (SUB-badge discrimination, Part chips via a future-`ROLE_VERSION` stub table, conflict-negative guard — all passed first-run, no latent defect). Final review (independent suite re-run + offscreen probes) With-Fixes → I1: the hidden Tools row was keyboard-reachable (End/Ctrl+End/type-ahead switched the stack while the nav highlight stayed put — QListView `MoveEnd`/`keyboardSearch` ignore hidden rows) — fixed in `cb24bd7`: item flags cleared (`~ItemIsEnabled & ~ItemIsSelectable`) + End-key regression assert + flags-restore reveal in the pinned test. Fast 948/948, smoke 269/269, zero skips. Task 6 grabs verified the check glyph, all six header icons, the Data page, the revealed Tools page, and the expansion card's SUB badge + Part chips + disabled Merge… .
- [ ] Plan 9: NOT written yet — write with `superpowers:writing-plans` against the landed Plan 8 code (final pass: §18 DPI 100/150/200% visual pass, real-library validation, string/perf sweep re-run)

## How to resume

1. **Next step: write Plan 9 (final pass — DPI 100/150/200% visual pass, real-library validation, string/perf sweep re-run, spec §18)** with `superpowers:writing-plans` against the landed Plan 8 code. Plan 9 is the roadmap's last plan — fold the accumulated minors below into its polish scope.
2. Plan 8 leftovers (full review text `.superpowers/sdd/plan8-final-review.md`, all Minor, deferred to Plan 9):
   - `settings_tab.py:1-6` module docstring still describes the pre-V4 section list ("…Display, Matching, API Keys, Cache, and Advanced…") — the reviewer's condition for deferring was exactly this carry-forward line; fix in the next commit touching that file.
   - `SettingsTab.__init__` callback params are positional-or-keyword ahead of `parent` — a five-positional-arg caller would bind its parent widget into `history_count_callback`; make the callbacks keyword-only on the next signature touch.
   - A declined confirm leaves a previous success caption visible (`_cache_confirm`/`_history_confirm` untouched on the No path) — clear the caption on decline.
   - Only the Destinations page's header icon is test-pinned; loop-pin all six pages' icons (incl. Tools' `SP_ComputerIcon`).
   - Packaging (pre-existing, not a Plan 8 regression): wheels would omit `resources/*` entirely (no package-data/MANIFEST) — the tmpl fails louder than the new SVG; note for whenever packaging is addressed.
   - Amended §13 seam note: revealing Tools is flags-restore + `setHidden(False)` (two lines, not one — see `cb24bd7`).
3. Plan 7 leftovers (full review text `.superpowers/sdd/plan7-final-review.md`):
   - Review Minor 3: `history_tab.py:50` annotates `parent: QWidget | None` without importing `QWidget` (harmless under `from __future__ import annotations`; pre-existing) — fix in the next commit that touches that file.
   - Grab-pass observations, both pre-existing, candidates for Plan 8/9 polish: the detail tree's season header says "(1 files)" for a single file (`_job_detail_preview.py` label f-string has no singular form), and the detail panel's "No Job Selected" placeholder card clips the last line of its hint text at default sizes.
   - Plan 7 recorded deviations stay on record, esp. deviation 4: companion↔video pairing is heuristic (same `target_dir_relative` + `new_name` starts with video target stem + `"."`, longest stem wins) — a merge-job implementation (§13) inherits this seam and its residual-group guarantee.
4. Deferred items from Plan 6's review, not taken during Plans 7-8 (full text `.superpowers/sdd/plan6-final-review.md`, all Minor):
   - **M1:** the "Queue Failed" side of the split envelope has no test (the plan assumed one existed) — add the companion: `add_batch` raises → box titled "Queue Failed", `queue_changed` NOT emitted.
   - **M2:** no test anywhere pins the `total == 0` → `"Working"` count-label state (the lifecycle-skip test adaptation dropped the only one) — re-add one assertion.
   - **M3:** a straggler `update_progress` after `stop()` restarts `_filler_timer` as a perpetual no-op — gate the restart on `_elapsed_timer.isActive()` or stop the timer inside `_rotate_filler`'s inactive guard.
   - **M4:** two bare px literals in the rebuilt toast card (`setFixedHeight(3)` countdown bar — brief-verbatim carry-over from the old card — and the `+ 4` clamp slack in `_sync_clamp`) — migrate to `_scale.px` in a polish pass.
   - **M5:** `test_scan_progress_conveyor_fills_cards_behind_the_beam` pins only tick gating, not fill — rename to `..._advances_only_while_active` or leave (fill-behind-beam was verified visually in Task 7's grabs).
   - **M6:** the conftest_qt pool drain (8 no-op futures) is not a strict barrier; empirically sufficient — if the interpreter-exit crash ever recurs, replace with a `threading.Barrier(9)` rendezvous wave. The production-side thread_pool atexit quit race was spawned as a separate user task during Batch A (out of GUI-plan scope).
5. Carried from Plan 5 (unchanged): `_rebuild` during a skeleton schedules a duplicate build (last-wins keeps it correct, waste only); a scan-error re-show doesn't bump the token, so a same-object delivery can render over the error row (pre-existing corner); sweep-guard needles don't cover `from time import sleep` / bare `sleep(` / `QThread.msleep(`; the "force rescan" overlay site doesn't exist in code; collapse/expand <50ms has no flake-safe guard.
6. Spec deviations on record from Plan 6 (see the plan's "Recorded deviations"): bulk-apply per-file failures exit the mode with a sticky error toast + counts + reason — spec §17 wanted the mode kept open with failed rows listed, which needs an `apply_assignments` signature change (§16 conflict); **OPEN QUESTION for the user**. Guide-build failures don't drive the roster `scan_error` pill (the view must not write engine state). Stepper active dot restyled warning→accent.
7. Accepted staleness boundary from Plan 4's review (M1 + M5 — this doc note IS the fix): bulk-assign mode pins the state it was entered on. A roster switch mid-staging auto-exits and discards staging (regression-test-pinned — don't "fix" the same-state-keeps-mode counterpart casually). A same-state background refresh keeps the mode with possibly-stale staging until Apply/Cancel (Apply re-validates through the service, so stale pairs are skipped, not misapplied). And an empty-roster refresh clears the panel without the discard hook, leaving the bulk page up with an orphaned pin until the next populate self-heals (M5, pre-existing family; scan flows hide the panel behind the scanning page anyway).
8. Resolved en route: Plan 2's carry-overs are gone — `set_item_check_state`'s vestigial `preview` param died with the preview panel, and `_clear_plex_ready_checks` → `_clear_fully_ready_checks` (it lived in `app/controllers/_tv_batch_helpers.py`, not the workspace file Plan 3's Task 6 named).
9. Still open from Plan 1's review (user decision, not scheduled): optionally broaden the no-hex guard regex to 3/8-digit hex (plan mandated `{6}`; zero such literals exist today).
10. Always update this file + the roadmap status column at each milestone.

## Open questions for the user (also in spec §15)

1. ~~App display name~~ — **RESOLVED 2026-07-03: "NameScraper"** (implemented in Plan 1).
2. Episode description + air date placement = expanded episode row; show overview = work-panel header (spec §3.2). Confirm.
3. Roster poster size ~64×94 logical. Confirm/adjust.
4. ~~Bulk Assign MVP scope~~ — **RESOLVED 2026-07-03: user approved Plan 4 as written** (spec §6 + §15.7 MVP boundary: staging panel-local, Reset/Cancel only, no in-mode undo; landed 2026-07-04).

## Key design decisions (spec is authoritative; summary)

- **Approach A**: model/view rebuild (QAbstractItemModel + QStyledItemDelegate, like the queue tab) replaces per-row live widgets; expanded row = single persistent-editor widget. Delete `warm_preview_cache`, render-key retention, all `processEvents` in workspace.
- **2-panel**: enlarged poster-forward roster (+season chips, new group taxonomy incl. `Specials & Unmapped Only`, `plex-ready`→`fully-ready`) + work panel (show header / season strip / toolbar / virtualized episode table with ghost missing-rows + in-place expansion / footer file-breakdown + Fix Match + Queue buttons). `MediaDetailPanel` deleted; movie mode gets same shell minus season strip.
- **Bulk Assign mode** in-panel (check files → assign-in-order to contiguous slots; auto-map remaining; apply once). Unassign All becomes danger-outline + confirm + bulk-assign offer.
- **Theme**: `gui_qt/theme.py` tokens + `theme.qss.tmpl`; Jellyfin palette (accent #00a4dc etc.); radii sm4/md8/lg12/pill10; ALL fringes removed (cards get ≤6% alpha tone washes); guard tests: no hex outside theme.py, no "Plex" string literals in gui_qt (AST-based, allowlists plex_renamer/PLEX_RENAMER identifiers).
- **Perf budget**: 300-ep show switch <100ms; no event-loop block >200ms without BusyOverlay.
- **mkvmerge**: seams only (disabled Merge… slot in expanded row, Files section layout, queue-detail grouping, hidden Settings "Tools" section). NOT wired.
- Ctrl+Z undo removed (Task 5, plan 1); recent-folder menus switch tab before load (Task 6, plan 1).

## Codebase findings (exploration complete — trust these, verified 2026-07-03)

- Workspace: `widgets/media_workspace.py` QStackedWidget EMPTY/SCANNING/READY; READY = QSplitter [320,540,380]: roster (`_media_workspace_roster.py`, per-row `RosterRowWidget` via setItemWidget), preview (`_media_workspace_preview.py`, `EpisodeGuideRowWidget` TV / `PreviewRowWidget` movie, render-cache keyed `id(state)` keeps hidden items of ALL shows in one QListWidget), detail (`media_detail_panel.py` — owns Fix Match + primary buttons, rebound in `_media_workspace_ui.py:_build_detail_panel`).
- Perf bugs: per-row live widgets; `processEvents` every 25 (roster) / 30 (guide) rows; `warm_preview_cache` (`_media_workspace_state.py:93`) synchronously pre-builds widgets for EVERY scanned show; full rebuilds on change; no busy indicator.
- Data for chips already exists: `engine/models.py` `CompletenessReport.seasons[n] = SeasonCompleteness{expected, matched, missing[]}` + specials; `app/models/state_models.py` `EpisodeGuide{rows, unmapped_primary_files, duplicate_files, orphan_companion_files, summary}`.
- Groups today (`_media_helpers.roster_group`): queued/plex-ready/matched/review-match/review-episodes/unmatched/duplicate. Status strings minted in `_media_helpers.state_status`. `is_plex_ready_state` → `CommandGatingService` (internal name, rename in Plan 2).
- Theme: `resources/theme.qss` ~1007 lines Plex amber (#e5a00d); ~65 more hexes across 11 py files (`_media_helpers` 21, `_workspace_widget_primitives` 14, `job_table_model` 7, `_image_utils` 6, `toast_manager` 5, `scan_progress` 5, `_job_list_tab` 3 + stripe painting, `tab_badge`/`job_detail_panel`/`_match_picker_selection`/`_media_detail_artwork` 1 each). Fringe sites: roster/preview cards + callout-banner (QSS), toast card (inline py), table selected-row stripe (`_job_list_tab.py` delegate).
- QSS loaded in `app.py:142-146` from `_THEME_PATH`; app name set at `app.py:137`; title `main_window.py:67`; About `_main_window_shell.py:71-79`.
- Shortcuts in `_main_window_chrome.py` (Ctrl+Z undo at lines 43-45); recent menus in `_main_window_state.py:86-105` (no tab switch — the bug).
- Queue/History already model/view (`_job_list_tab.py` + `models/job_table_model.py` + `job_status_filter_proxy_model.py`) — pattern to copy for workspace.
- Tests: fast sweep `scripts\test-fast.cmd`; Qt smoke `scripts\test-smoke.cmd` (log at `.pytest_cache/smoke/latest.log`). Tests referencing `plex-ready`: `test_qt_queue_history.py:612`, `test_qt_media_workspace.py:3716`. Real-library harness `scripts/scan_real_library.py` (needs P: drive; exits 2 if missing).
- Scaling: `gui_qt/_scale.py` (`px()` logical-DPI). Keep for everything.

## Session log

- 2026-07-03: branch created → exploration → spec written + self-reviewed → committed 32cee9a → roadmap + Plan 1 written → this handoff updated → committed (see git log) → session ended at user review gate.
- 2026-07-03 (later): user approved (app name "NameScraper", ≤4 subagents) → Plan 1 executed via subagent-driven development (3 implementer batches: tasks 1-2, 3, 4-6; 1 final whole-branch reviewer on `380d5f7..d421d37`) → review verdict "ready to merge", 0 Critical/Important, minors deferred to Plan 2 (listed under "How to resume") → full suites green (fast 928/928, smoke 163/163) + offscreen themed-window screenshot sanity check → roadmap/handoff updated. Notable during execution: plan's Task 5 test snippet had a shiboken object-lifetime bug (root-caused, fixed in `tests/test_qt_chrome.py`); `tab_badge.failure_visible()` hex-substring check fixed so it keeps working post-tokenization; `.superpowers/` added to `.gitignore` (SDD scratch).
- 2026-07-03 (later still): Plan 2 (roster) written via superpowers:writing-plans against the landed Plan 1 code — codebase re-explored (roster/coordinator touchpoint map, queue-tab model/view pattern, CompletenessReport/assignment-table shapes), 7 TDD tasks incl. cutover migration tables and Plan 1 carry-overs, self-reviewed, committed. Session paused offering execution options for Plan 2.
- 2026-07-03 (evening): user approved subagent-driven execution (fresh ≤4 budget) → Plan 2 executed (batches: tasks 1-3 / 4-5 / 6-cutover; final opus review). Reviewer found 1 Important (chip-tooltip helpEvent used view font vs painted chip font — plan gap, unguarded) → controller fixed in `e3bb6de` with a discriminating regression test + compact-title elision + poster-transition test; re-review by the same reviewer: "Ready to merge — Yes". Two plan defects surfaced by implementers, both resolved and documented in reports: the Task-5 toggle-gate fixture could never be checkable (gate moved to panel; small dead-click zone accepted by review), and the runner-classification lists live under `scripts/` not `tests/`. Batch-A agent crashed on a transient API error post-commit and was resumed for handoff. Task 7 carry-overs by controller; roster visual grab verified chips/pills/washes/selection. SDD scratch in `.superpowers/sdd/` (reports, reviews, ledger).
- 2026-07-03 (night): Plan 3 (work panel) written via superpowers:writing-plans — preview/detail/action modules fully explored first (preview panel render-cache + guide sections, detail payload/workflow async pattern, action-bar alias system, EpisodeGuideRow already carries overview/air_date, guide already emits Missing File ghost rows), 7 TDD tasks with cutover touchpoint map + test-migration pattern table, self-reviewed (added §17 scan-error row, fixed editor mechanics split between delegate/panel), committed. Session paused offering execution options.
- 2026-07-03 (late night, session resumed after interruption): Plan 3 executed via subagent-driven development (batches: tasks 1-2 / 3-4 on sonnet, task-5 cutover + final review on opus; final review re-dispatched post-interruption over `3178400..0392c29` with the test-drop audit, expansion lifecycle trace, and 4 cutover deviations — all adjudicated KEEP, test drops fully reconciled). Review found 1 Important (expansion sentinel survived `_rebuild` → stale blank gap after search/filter/collapse) — fixed with two minors in `8269b79`, re-review clean. Task 6 guard + rename `e6fb4f3` (plan defect: the identifier lived in `app/controllers/_tv_batch_helpers.py`). Task 7 verification then caught what all reviews missed: `summary_text` violated the exact footer constraint — the plan's own degenerate fixture (companions=0, duplicates=0) made wrong and right logic identical (`06106b2`); `createEditor` dropped its parent arg so the expansion card floated as a detached top-level window while the expanded row painted a blank gap — proven with native `grabWindow` + parentage introspection, invisible to interaction tests (`e3e7f57`); the status-pill QSS still used the legacy `accent` selector while the header remaps accent→warning, leaving the pill unstyled (same commit; tone-vocabulary guard added). Visual grabs verified post-fix (main + inline expansion card). Final suites: fast 927/927, smoke 188/188, zero skips. SDD artifacts in `.superpowers/sdd/` (ledger, reports, final review with 3 re-review addenda).
- 2026-07-03 (continued): Plan 4 (bulk assign) written via superpowers:writing-plans against the landed Plan 3 code — mapping service / assignment table / work-panel toolbar / assign-dialog / toast plumbing explored first (`episode_slot_choices` + `unassigned_file_previews` already feed both bulk panes; `unclaimed_slots` exists for auto-map; `table.assign` validates contiguity + slots and displaces; `status_message` is a statusbar line, so the spec's "one toast" gets a new `toast_requested` signal into the existing ToastManager; assign-dialog leaves already tooltip, only the header label elides). 7 TDD tasks with exact code, self-reviewed (spec §6/§15.7 coverage, placeholder sweep with real fixture idioms from `test_qt_media_workspace.py`/`test_manual_assign_queueable.py`, type-consistency pass), committed. Session paused offering execution options.
- 2026-07-03 → 2026-07-04 (approved, executed across an interruption): Plan 4 executed via subagent-driven development (batches: tasks 1-2 / 3-4 / 5-6 on sonnet, final review on opus). Mid-execution the batch-B agent sat on a smoke "hang" the controller root-caused: a pre-existing unassign-all test clicked the now-real confirm modal under offscreen (`box.exec()` blocks forever) — a plan defect in Task 4's migration list; the test now patches the box to Yes. Final review verdict With-Fixes: I1 `apply_bulk_assignments` missing `assignments is None` guard after it had already exited bulk mode; I2 Apply re-read `_selected_state()` so a roster switch mid-staging would commit staged pairs against the wrong show. Session interrupted mid-fix-wave with the fix uncommitted; the resumed session's fix agent root-caused the inherited failing regression test (it drove the switch via the programmatic `_set_roster_current_state`, whose `_syncing` guard suppresses `state_selected` entirely — re-pointed at the roster view's selection model, the genuine user path; the production pin/guard/hook were already on the right seams) and landed `eb8ee91`. Re-review flipped the verdict to Ready to merge — Yes, adding M5 (empty-roster refresh bypasses the discard hook; accepted into the M1 staleness family — both recorded under "How to resume" #3). M2 (slot-pane season header counts claimed+missing next to "Unassigned files (N)") adjudicated spec-faithful, no action. Task 7: fast 931/931 + smoke 209/209, zero skips; offscreen visual grabs verified the problems-filter hint row, the unstaged bulk page (Apply disabled), and the staged page (accent `→` markers; claimed/staged/missing slot tones; Assign-in-order/Auto-map/Reset row) plus parentage assertions — no detached top-levels (Plan 3's lesson). SDD artifacts in `.superpowers/sdd/`.
- 2026-07-04 (continued): Plan 5 (async + perf) written via superpowers:writing-plans against the landed Plan 4 code — guide pipeline explored first (`EpisodeTableModel.show_state` pulls the guide synchronously inside a model reset; `EpisodeProjectionCacheService.guide_for_state` builds on miss; the work panel's overview fetch is the proven bridge+token async pattern to copy; `plex_renamer/thread_pool.py` is the spec's "existing thread pool"; repo-wide `processEvents` grep already clean since Plan 3; strip churn confirmed at `_refresh_strip` via `update_action_bar` → `refresh_header`). 7 TDD tasks with exact code and verified token/idiom names (`bg`/`surface`/`accent`/`text_dim`, `from conftest_qt import QtSmokeBase`, runner-list line formats), self-reviewed (spec §7 coverage incl. two recorded deviations: no "force rescan" surface exists; collapse/expand <50ms has no flake-safe guard), committed. Session paused offering execution options.
- 2026-07-04 (approved, executed across an interruption): Plan 5 executed via subagent-driven development, all agents fable per user cap (batches: tasks 1-2 / 3-4 / 5-6, then final whole-branch review over `c399802..c4be29b`). Batch B flagged the toolbar-over-bulk defect in its own diff rather than silently "improving" the brief; the final review confirmed it as I2 and found its sibling I1 (cache-hit re-render kept the guide token, so a build made stale by a mid-flight mutation could render pre-mutation rows and clobber the fresh signature-checked cache entry) — both plan defects implemented verbatim. Fix wave landed `3765d5a` (unconditional bump at the top of `_resolve_guide_or_schedule` — deliberately orphaning in-flight builds on every resolution — plus the `bulk_assign_active()` guard in `_on_guide_loaded` and a files-scanned counter hardening the perf sweep guard) with RED reproducing both predicted failure signatures. The session died awaiting the re-review; the resumed session re-dispatched a fresh fable reviewer with pointed probes — bump-vs-pairing invariant (could an orphaned build strand a skeleton?), post-exit toolbar staleness across all four bulk exits, and regression-test vacuousness — verdict: Ready to merge — Yes, with one NEW pre-existing Minor deferred to Plan 6 (same-state repopulate mid-bulk re-shows Unassign All via `show_state`'s unconditional `update_toolbar`). Task 7: fast 936/936 + smoke 229/229 zero skips; offscreen grabs verified skeleton ("Loading episodes…" band + dim bars over the themed panel), loaded rows + footer, and the immediate `busy_scope` scrim/spinner/label — parentage assertions clean, no detached top-levels (Plan 3's lesson holds). SDD artifacts in `.superpowers/sdd/` (ledger, reports, final review + re-review addendum).
- 2026-07-04 (continued): Plan 6 (toasts + loading screen) written via superpowers:writing-plans against the landed Plan 5 code, then executed across TWO interruptions. Sessions 1-2 landed Tasks 1-4 via fable batch agents (A: toast card rebuild + behaviors `eb5ea11`/`3a7003f`; B: conveyor+stepper + status lines/quips `ac8da40`/`5ecfe5f`) — notable en route: a pre-existing thread_pool atexit race caused intermittent 0xC0000005 at interpreter exit (pool drain added to `QtSmokeBase.tearDownClass`, production fix spawned as a user task), and Task 4's brief-verbatim code failed its own quips test against the pre-existing 650ms text throttle (one-line fix: `_rotate_filler` invalidates the throttle timer — later endorsed by the final review).
- 2026-07-04 (batch C resume): the dead session had left Task 5's failing tests uncommitted; controller finished Task 5 inline (`bd40463` — guide-failure error row via new `guide_failed` bridge signal with token-staleness guard + retry-on-reselect, "Loading episodes…"/"Guide unavailable" footers, "Queued With Warnings" split envelope that still emits `queue_changed`, error-tone bulk failure toast with the skip reason). User granted 3 fable subagents: the Task 6 implementer landed the `update_toolbar` bulk gate (`033b0fc`, RED→GREEN); the final whole-branch review over `764cda6..033b0fc` returned With-Fixes — I1: the plan's verbatim `summary_text` snippet gave scan-error states a false "Loading episodes…" footer and a path-dependent stale "Guide unavailable" (probe-confirmed plan defect; flagged by the controller during Task 5 and queued for adjudication) — controller fixed it with the reviewer's `scan_error` guard + a stale-flag pinning test (`6793ccf`); re-review by the same reviewer probed both variants and the test's discrimination against pre-fix code: Ready to merge — Yes. 6 Minors deferred (see "How to resume" #4). Task 7: fast 940/940 + smoke 249/249, zero skips; offscreen grabs (21 programmatic checks + image review) verified the sticky clamped/expanded error toast (tone icon, Copy, Show more/less, 40% cap, no countdown), the conveyor (filled wash behind the accent beam) + stepper (done/active/pending) + honest primary line + quip secondary, and the failed-guide error row + "Guide unavailable" footer; parentage clean, no stray top-levels. SDD artifacts in `.superpowers/sdd/`.
- 2026-07-04 → 2026-07-05 (Plan 7 written + executed across an interruption): Plan 7 (queue/history restyle + companion surfacing) written via superpowers:writing-plans against the landed Plan 6 code (5 deviations recorded up front — three §11 bullets already landed in Plan 1, pairing is a view-side heuristic, facts card keeps both cells), then executed via subagent-driven development (batches: tasks 1-2 / 3-4 / 5-6, reports in `.superpowers/sdd/`). En route the batch-B implementer caught a plan defect: Task 4's verbatim code made video rows the first non-header tree items with children, so the pre-existing `childCount() > 0` expand/collapse handlers wrote accumulating "▸ ▾ " arrow prefixes over video rows — fixed in `53e9900` with an identity predicate (`_is_preview_group_header` anchors on the group-header UserRole, whose only writer is `create_preview_group_header`) in both toggle paths + a non-vacuous regression. The session died right after the final whole-branch review (fresh reviewer over `ae10f32..254d02d`, with an independent offscreen pill-geometry probe) landed in `.superpowers/sdd/plan7-final-review.md`: With-Fixes — I1: the Status section was sized from the mixed-case display text (no delegate `sizeHint`), so ResizeToContents squeezed COMPLETED/CANCELLED pill padding to ~2.5px and clipped ~8px of REVERT FAILED's uppercase glyphs. The resumed session verified the mechanism at HEAD and TDD'd the fix (`bd9d579`): `sizeHint` override reserving uppercase advance + 2×8px pad + the 4px margin the paint clamp subtracts, pill height pinned to `_scale.px(18)` for workspace `_PILL_H_U` parity (review M2), a REVERT_FAILED regression test, and `import unittest` en passant (review M1; M3's `QWidget` annotation deferred to the next `history_tab.py` commit). Task 7: fast 947/947 + smoke 261/261, zero skips; offscreen grabs (25 programmatic checks + image review; grab set extended with REVERT_FAILED/CANCELLED per review Rec 2) verified all five history pills padded and unclipped — Status section 106px = the exact widest-pill requirement at the real QSS font — plus accent title + 36px rows + merged Files column, the armed warning banner, nested SUB badges under an expanded season (arrow-prefix guard exercised, video rows stay arrow-free), and all three empty-state variants; parentage clean, no stray top-levels. Two pre-existing polish nits observed and recorded under "How to resume" #3 ("(1 files)" season label; clipped "No Job Selected" hint).
- 2026-07-05 (continued): Plan 8 (settings restyle + mkvmerge seams) written via superpowers:writing-plans against the landed Plan 7 code — settings stack explored first (`settings_tab.py` nav+stack shell, `SettingsSectionCard` heading+hairline in `_settings_tab_sections.py`, injected-`message_box_api` confirm pattern in `_settings_tab_actions.py`, `PersistentCacheService.stats()["item_count"]` for pre-clear counts, `_clear_history_from_settings` wiring in `_main_window_tabs.py`), guard mechanics verified (hex guard scans `gui_qt/*.py` + the tmpl, so the new check.svg uses `stroke="white"`; `${check_svg}` joins the strict `Template.substitute` mapping), and the §13 seam inventory reconciled: expanded-row badges/disabled-Merge…/Part-chips landed in Plan 3 (`_episode_expansion.py`; Merge… already pinned by `test_episode_expansion.py:26`), queue grouping landed in Plan 7, so the only new §13 code is the hidden Settings Tools shell. Found en route: `_multi_part_chip_specs` is a **latent** seam — today's engine policy (`episode_assignments.py`: 2+ claims on a slot = conflict, all claimants conflicted) means the Part-chips branch can only fire under the future `ROLE_VERSION` policy, so Plan 8 Task 5 pins the view contract with a stub table (2 claims, none conflicted) plus a conflict-negative test. 6 TDD tasks with exact code, self-reviewed (spec §12/§13 coverage table, fixture counts cross-checked against the real cache test, `ChipSpec`/`Assignment`/`CompanionFile` field names verified in source), committed. Session paused offering execution options.
- 2026-07-05 (approved, executed same session): Plan 8 executed via subagent-driven development, all agents fable per user cap (batches: tasks 1-2 / 3-4 / 5 with controller diff-reviews between batches, then a final whole-branch fable review over `d9d06ba..6464b8e` with six pointed probes). All three batches came back DONE with clean controller reviews (notable: the Task 5 pins all passed first-run, so the latent Part-chips seam carries no hidden defect; batch B's grep sweep required renaming the `# ── Data Management ──` section comment — adjudicated KEEP). The final review independently re-ran both suites (exact match), applied the full stylesheet offscreen with zero Qt parse warnings, probed the confirm-flow matrix incl. TOCTOU honesty, and verified the Task 5 stubs hit the real `_multi_part_chip_specs` predicate — verdict With-Fixes: I1, the hidden Tools nav row was keyboard-reachable (`End`/`Ctrl+End`/type-ahead `T` switched the stack to the reserved page while the nav highlight stayed on the previous row; QListView `MoveEnd`/`keyboardSearch` don't honor hidden rows, only `MoveDown` does). Controller fixed it inline (`cb24bd7`, budget spent): RED reproduced the leak via an End-key assert, fix clears the hidden item's `ItemIsEnabled|ItemIsSelectable` flags, the pinned reveal test now restores flags first — reviewer's own probe had pre-verified this exact fix. Reviewer adjudications: Task 2's RED-name mismatch KEEP (assertion-order artifact); comment rename KEEP; stale `settings_tab.py` docstring rides to Plan 9 conditional on the carry-forward entry (done, "How to resume" #2). Task 6: fast 948/948 + smoke 269/269, zero skips; offscreen grabs (24 programmatic checks + image review) verified the SVG check glyph rendering checked+unchecked, all six page header icons (review Minor 5's manual coverage), the merged Data page with danger-outline pair + live stats, the revealed Tools page, the End-key regression at the real widget, and the expansion card's SUB badge + "Part 1 · Part 2" chips + disabled Merge… in one scene; parentage clean. SDD artifacts in `.superpowers/sdd/` (ledger, three reports, final review).
